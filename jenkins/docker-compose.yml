version: '3'
services:
  purple_clouds_jenkins:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
        - 8080:8080
        - 50000:50000
    volumes:
      - /data/jenkins:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock